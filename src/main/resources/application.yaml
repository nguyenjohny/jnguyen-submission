spring:
  datasource:
#    url: jdbc:h2:file:./.local/dbStore/demo
    url: jdbc:h2:mem:robotoperations
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console.enabled: true
robot:
  default: commands.txt